{{- if .Values.operations.enabled -}}

apiVersion: apps/v1
kind: Deployment
metadata:
  name: ph-ee-importer-rdbms
  labels:
    app: ph-ee-importer-rdbms
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ph-ee-importer-rdbms
  template:
    metadata:
      labels:
        app: ph-ee-importer-rdbms
    spec:
      containers:
        - name: ph-ee-importer-rdbms
          image: "{{ .Values.importer_rdbms.image }}"
          resources:
            limits:
              memory: "512M"
          ports:
            - containerPort: 8000
          env:
            - name: "SPRING_PROFILES_ACTIVE"
              value: "{{ .Values.operations.SPRING_PROFILES_ACTIVE }}"
          volumeMounts:
            - name: ph-ee-config
              mountPath: "/config"
      volumes:
        - name: ph-ee-config
          configMap:
            name: ph-ee-config

{{- end }}
