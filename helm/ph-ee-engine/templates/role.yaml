kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  namespace: default
  name: ph-ee-operator-c-role
rules:
  - verbs:
      - get
    apiGroups:
      - ''
    resources:
      - pods
      - pods/log