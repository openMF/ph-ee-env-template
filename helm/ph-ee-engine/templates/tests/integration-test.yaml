apiVersion: v1
kind: Pod
metadata:
  name: "g2p-sandbox-test-connection"
  labels:
    app: "integration-test"
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['www.google.com']
  restartPolicy: Never
