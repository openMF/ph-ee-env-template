{{- if .Values.enabled -}}
apiVersion: {{ .Values.service.apiversion }}
kind: Service
metadata:
  labels:
    app: ph-ee-integration-test-job
  name: ph-ee-integration-test-job
spec:
  ports:
    - name: port
      port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app: ph-ee-integration-test-job
  sessionAffinity: None
  type: ClusterIP
{{- end }}