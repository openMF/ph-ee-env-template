# Deploy singleton instance in the whole cluster for some unique data sources, like aws input
{{- if .Values.kafka.enabled -}}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "kafka.fullname" . }}
  labels:
    app: "{{ template "kafka.fullname" . }}"
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    heritage: '{{ .Release.Service }}'

  {{- if .Values.kafka.deployment.annotations }}
  annotations:
    {{- range $key, $value := .Values.deployment.annotations }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
  {{- end }}
spec:
  replicas: {{ .Values.kafka.replicas }}
  selector:
    matchLabels:
      app: "{{ template "kafka.fullname" . }}"
  template:
    metadata:
      annotations:
{{- if .Values.kafka.deployment.annotations }}
{{ toYaml .Values.kafka.deployment.annotations | indent 8 }}
{{- end }}
        configChecksum: {{ include (print .Template.BasePath "/configmap.yaml") . | sha256sum | trunc 63 }}
        {{- end }}
      labels:
        app: '{{ template "kafka.fullname" . }}'
        chart: '{{ .Chart.Name }}-{{ .Chart.Version }}'
    spec:
      affinity: {{ toYaml .Values.kafka.deployment.affinity | nindent 8 }}
      nodeSelector: {{ toYaml .Values.kafka.deployment.nodeSelector | nindent 8 }}
      tolerations: {{ toYaml ( .Values.kafka.tolerations | default .Values.kafka.deployment.tolerations ) | nindent 8 }}
      {{- if .Values.kafka.priorityClassName }}
      priorityClassName: {{ .Values.kafka.priorityClassName  }}
      {{- end }}
      serviceAccountName: {{ template "kafka.serviceAccount" . }}
      terminationGracePeriodSeconds: {{ .Values.kafka.terminationGracePeriod }}
      {{- if .Values.kafka.deployment.hostAliases }}
      hostAliases: {{ toYaml .Values.kafka.deployment.hostAliases | nindent 8 }}
      {{- end }}
      volumes:
      {{- range .Values.kafka.secretMounts | default .Values.kafka.deployment.secretMounts }}
      - name: {{ .name }}
        secret:
          secretName: {{ .secretName }}
      {{- end }}
      {{- if .Values.kafkaConfig }}
      - name: kafka-config
        configMap:
          defaultMode: 0600
          name: {{ template "kafka.fullname" . }}-config
      {{- if .Values.kafka.extraVolumes | default .Values.kafka.deployment.extraVolumes }}
{{ toYaml ( .Values.kafka.extraVolumes | default .Values.kafka.deployment.extraVolumes ) | indent 6 }}
      {{- end }}

      {{- if .Values.kafka.imagePullSecrets }}
      imagePullSecrets:
{{ toYaml .Values.kafka.imagePullSecrets | indent 8 }}
      {{- end }}
      {{- if .Values.kafka.extraInitContainers }}
      initContainers:
      # All the other beats accept a string here while
      # kafka accepts a valid yaml array. We're keeping
      # this as a backwards compatible change, while adding
      # also a way to pass a string as other templates to
      # make these implementations consistent.
      # https://github.com/elastic/helm-charts/issues/490
      {{- if eq "string" (printf "%T" .Values.extraInitContainers) }}
{{ tpl .Values.extraInitContainers . | indent 6 }}
      {{- else }}
{{ toYaml .Values.extraInitContainers | indent 6 }}
      {{- end }}
      {{- end }}
      containers:
      - name: "kafka"
        image: "{{ .Values.kafka.image }}:{{ .Values.kafka.imageTag }}"
        imagePullPolicy: "{{ .Values.kafka.imagePullPolicy }}"
        ports:
          - containerPort: 5000        
        resources:
            limits:
              memory: "{{ .Values.kafka.limits.memory }}"
              cpu: "{{ .Values.kafka.limits.cpu }}"
            requests:
              memory: "{{ .Values.kafka.requests.memory }}"
              cpu: "{{ .Values.kafka.requests.cpu }}"
        args:
          - "-e"
          - "-E"
          - "http.enabled=true"
        livenessProbe:
{{ toYaml .Values.kafka.livenessProbe | indent 10 }}
        readinessProbe:
{{ toYaml .Values.kafka.readinessProbe | indent 10 }}        
        env:
            - name: "SPRING_PROFILES_ACTIVE"
              value: "{{ .Values.paygops_connector.SPRING_PROFILES_ACTIVE }}"
            - name: "ZEEBE_BROKER_CONTACTPOINT"
              value: "zeebe-zeebe-gateway:26500"
            - name: "AMS_LOCAL_ENABLED"
              value: "{{ .Values.paygops_connector.ams.local.enabled }}"
            - name: "PAYGOPS_AUTHHEADER"
              value: null
              valueFrom:
                secretKeyRef:
                  name: "paygops-secret"
                  key: "auth-token"
            - name: "PAYGOPS_BASE-URL"
              value: "{{ .Values.paygops_connector.paygops.base_url }}"
            - name: "LOGGING_LEVEL_ROOT"
              value: "{{ .Values.paygops_connector.LOGGING_LEVEL_ROOT }}"
{{- if .Values.kafka.extraEnvs | default .Values.kafka.deployment.extraEnvs }}
{{ toYaml ( .Values.kafka.extraEnvs | default .Values.kafka.deployment.extraEnvs ) | indent 8 }}
{{- end }}
        envFrom: {{ toYaml ( .Values.kafka.envFrom | default .Values.kafka.deployment.envFrom ) | nindent 10 }}
        securityContext: {{ toYaml ( .Values.kafka.podSecurityContext | default .Values.kafka.deployment.securityContext ) | nindent 10 }}
        volumeMounts:
        {{- range .Values.secretMounts | default .Values.kafka.deployment.secretMounts }}
        - name: {{ .name }}
          mountPath: {{ .path }}
          {{- if .subPath }}
          subPath: {{ .subPath }}
          {{- end }}
        {{- end }}
        {{- range $path, $config := .Values.kafkaConfig }}
        - name: kafka-config
          mountPath: /usr/share/kafka/{{ $path }}
          readOnly: true
          subPath: {{ $path }}
        {{ else }}
        {{- range $path, $config := .Values.kafka.deployment.kafkaConfig }}
        - name: kafka-config
          mountPath: /usr/share/kafka/{{ $path }}
          readOnly: true
          subPath: {{ $path }}
        {{- end }}
        {{- end }}
        {{- if .Values.kafka.extraVolumeMounts | default .Values.kafka.deployment.extraVolumeMounts }}
{{ toYaml ( .Values.kafka.extraVolumeMounts | default .Values.kafka.deployment.extraVolumeMounts ) | indent 8 }}
        {{- end }}
      {{- if .Values.kafka.extraContainers }}
{{ tpl .Values.kafka.extraContainers . | indent 6 }}
      {{- end }}
{{- end }}