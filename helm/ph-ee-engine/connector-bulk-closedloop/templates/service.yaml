apiVersion: {{ .Values.service.apiversion }}
kind: Service
metadata:
  labels:
    app: ph-ee-connector-bulk-mojaloop
  name: ph-ee-connector-bulk-mojaloop
spec:
  ports:
    - name: port
      port: 80
      protocol: TCP
      targetPort: 5000
  selector:
    app: ph-ee-connector-bulk-mojaloop
  sessionAffinity: None
  type: ClusterIP